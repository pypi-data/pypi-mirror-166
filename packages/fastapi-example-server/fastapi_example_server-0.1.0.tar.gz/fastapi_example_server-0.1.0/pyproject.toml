[tool.poetry]
name = "fastapi_example_server"
version = "0.1.0"
description = ""
authors = [
  "Julian Lechner"
]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10.6"
fastapi = {extras = ["all"], version = "^0.81.0"}
Faker = "^14.2.0"

[tool.poetry.dev-dependencies]
pytest = "^7.1.2"
pre-commit = "^2.20.0"
furo = "^2022.6.21"
Sphinx = "^5.1.1"
toml = "^0.10.2"

[tool.poetry.scripts]
main = "fastapi_example_server.main:main"
doc = "fastapi_example_server.doc:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
