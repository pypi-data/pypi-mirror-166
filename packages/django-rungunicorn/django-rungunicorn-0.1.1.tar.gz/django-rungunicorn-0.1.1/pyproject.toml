[tool.poetry]
name = "django-rungunicorn"
packages = [
  { include = "rungunicorn", from = "src" },
]
version = "0.1.1"
description = "Django management command starting gunicorn web server."
authors = ["Jarosław Wygoda <jaroslaw@wygoda.me>"]
license = "MIT"

repository = "https://github.com/jwygoda/django-rungunicorn"

[tool.poetry.dependencies]
python = "^3.8"
Django = "^4.0"
gunicorn = "^20.1.0"

[tool.poetry.dev-dependencies]
black = "^22.6.0"
isort = "^5.10.1"
flake8 = "^5.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
