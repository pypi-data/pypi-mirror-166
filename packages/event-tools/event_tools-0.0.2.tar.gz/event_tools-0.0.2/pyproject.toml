[project]
name = "event-tools"
dependencies = ["numpy>=1.22.4", ]

[build-system]
requires = ["maturin>=0.12,<0.13", "numpy>=1.22.4"]
build-backend = "maturin"
binding = "pyo3"
cargo-extra-args = "--features python"