[tool.poetry]
name = "chico"
version = "1.0.0"
description = "utils package"
readme = "README.md"
authors = ["Hung-Lin, Chen <hungln59638@gmail.com>"]
license = "GPL-3.0-or-later"

[tool.poetry.dependencies]
python = ">=3.7,<4.0"
SQLAlchemy = "^1.4.40"

[tool.poetry.dev-dependencies]
black = "^22.8.0"
ipykernel = "^6.15.2"
poetry2setup = "^1.0.0"
twine = "^4.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
